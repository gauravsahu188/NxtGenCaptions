version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - npm ci
run:
  command: npx tsx src/server.ts
  network:
    port: 3001
    env: PORT
  env:
    - name: NODE_ENV
      value: production
